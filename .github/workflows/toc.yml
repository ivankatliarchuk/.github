name: toc generator

on: push

jobs:
  generateTOC:
    name: toc generator
    runs-on: ubuntu-latest
    steps:
      - uses: technote-space/toc-generator@v4
        with:
          TARGET_PATHS: "CONTRIBUTING.md"
